# ✅ Simple Todo List App (TypeScript)

## 📘 Project Description
This is a simple **Todo List application** built using **TypeScript, HTML, and JavaScript**.  
It allows users to:
- Add new tasks  
- Edit existing tasks  
- Mark tasks as done  
- Delete tasks  

The project demonstrates basic **TypeScript concepts** such as interfaces, type safety, and DOM manipulation.  
It is compiled into JavaScript using the TypeScript compiler (`tsc`) and runs directly in the browser.

---

![alt text](/page.png)

---

## 🧩 Features
- ➕ Add tasks easily using the input box  
- ✏️ Edit existing tasks  
- ✅ Mark tasks as completed (with strike-through styling)  
- 🗑️ Delete tasks  
- 🖱️ Interactive right-click–like context menu for each task  
- 🚫 Displays a friendly message when there are no tasks  

---

### 1. Clone or Download the Project
If you have Git installed:
```bash
git clone https://github.com/yourusername/todo-typescript.git
cd todo-typescript
